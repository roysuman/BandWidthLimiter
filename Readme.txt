1. Installation: [ lib: g++ on UBUNTU ]
   a. tar -xvzf resilience.tar.gz
   b. cd  resilience
   c. make 

2. Execution:
   ./resilience [ options] [ prog Name]
   Example: ./resilience -i 127.0.0.1 -p 5000 -u 200 -d 200 -c "program to executue" -g "kernel glibc path"


3. ManPage
   man ./resman
